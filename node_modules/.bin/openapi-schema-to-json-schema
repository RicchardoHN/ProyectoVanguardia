#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@openapi-contrib/openapi-schema-to-json-schema/dist/bin.js" "$@"
else 
  exec node  "$basedir/../@openapi-contrib/openapi-schema-to-json-schema/dist/bin.js" "$@"
fi
